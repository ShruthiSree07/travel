APP_NAME=WanderFive
APP_TAGLINE=Type a place. Get 5 worth visiting.
PORT=8000

# Optional: set this to get live, LLM-generated recommendations for ANY
# location. Without it, the app uses a curated dataset of popular
# destinations plus a generic fallback — no key required to run.
ANTHROPIC_API_KEY=
